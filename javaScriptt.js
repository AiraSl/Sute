function whCalc (element){
	var fullId = "#"+element;
	var width = $("fullId").width();
	var height =$("fullId").height();
	$("#for results").("the height of the element textis " + height + " and the width  is " +
	width");
}
whCalc("div_form_2");